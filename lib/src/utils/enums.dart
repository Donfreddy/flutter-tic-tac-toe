enum GameDifficulty { easy, medium, hard }

enum GameMode { solo, multi }

enum GameState { blank, x, o }

enum BoardState { done, play }

enum PlayerType { player1, player2 }

enum WinnerType { none, player1, player2 }

enum Player { human, blank, computer }
